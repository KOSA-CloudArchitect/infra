# =============================================================================
# Test Environment Configuration
# =============================================================================
# 이 파일을 terraform.tfvars로 복사하고 실제 값으로 수정하세요

# =============================================================================
# 기본 설정
# =============================================================================

# AWS Region
aws_region = "ap-northeast-2"

# Project Name
project_name = "hihypipe"

# Environment
environment = "test"

# Owner
owner = "hihypipe-team"

# Cost Center
cost_center = "hihypipe-network"

# =============================================================================
# 네트워크 설정
# =============================================================================

# VPC CIDR Blocks (private와 다른 대역 사용)
vpc_db_cidr  = "172.20.0.0/17"    # VPC-DB용
vpc_app_cidr = "172.20.128.0/17"  # VPC-APP용

# On-premises CIDR
onprem_cidr = "10.128.0.0/19"

# =============================================================================
# RDS PostgreSQL 설정
# =============================================================================

# RDS PostgreSQL 생성 여부
create_rds_postgresql = true

# 데이터베이스 비밀번호 (필수 설정)
db_password = "YourSecurePassword123!"

# =============================================================================
# 단방향 통신 설정
# =============================================================================

# PUBLIC → ONPREM 통신은 허용되지만, ONPREM → PUBLIC은 차단됨
# 별도의 VPN 서버 설정이 필요하지 않음
# AWS의 기본 보안 정책에 의해 자동으로 적용됨

# =============================================================================
# 비용 최적화 설정
# =============================================================================

# 참고: 이 환경은 테스트용으로 최소 사양으로 설정되어 있습니다
# - RDS: db.t3.micro (최소 사양)
# - VPN Server: t3.micro (최소 사양)
# - 스토리지: 20GB (최소)
# - 성능 인사이트: 비활성화 (비용 절약)
# - 모니터링: 비활성화 (비용 절약)
