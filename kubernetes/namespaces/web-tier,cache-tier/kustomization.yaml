# kubernetes/namespaces/web-tier,cache-tier/kustomization.yaml

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
# 👇 아래 경로들을 추가하여 Secret, ConfigMap, Application이 모두 배포되도록 합니다.
- ../web-tier/02-secrets
- ../web-tier/03-configs
- ./04-applications # 기존 애플리케이션 경로
