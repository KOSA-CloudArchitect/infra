# kubernetes/namespaces/web-tier,cache-tier/kustomization.yaml

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 👇 여기에 secrets, configs 폴더 경로를 추가해야 합니다.
resources:
- ../web-tier/02-secrets  # Secret 리소스들이 있는 경로
- ../web-tier/03-configs  # ConfigMap 리소스들이 있는 경로
- ./04-applications     # Application Deployment가 있는 경로
